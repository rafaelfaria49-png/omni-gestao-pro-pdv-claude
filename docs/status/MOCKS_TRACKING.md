---
title: Mocks Tracking — onde ainda há mock no projeto
status: vivo
owner: produto + Sonnet
last_update: 2026-08-21
---

# 🎭 Mocks Tracking

> **Por que este arquivo existe:** um dos inegociáveis da governança é "**nunca criar mocks enganosos que pareçam persistência real**".
> Quando mock for inevitável (UI Lovable, demo, hub novo greenfield), **registrar aqui** para virar dívida controlada.

> **Atualização:** ao criar mock (registrar) ou ao removê-lo (mover para §3).

---

## 1. Convenção

### 1.1 Tipos
- **UI-mock** — componente renderiza dado fake, sem persistência
- **API-mock** — endpoint devolve dado fake (ex: durante desenvolvimento)
- **Seed-mock** — dados de exemplo para demo/onboarding
- **Hub-mock** — HUB inteiro ainda em mock (greenfield ou Lovable)

### 1.2 Risco
- 🔴 alto — usuário confunde com real, decisão errada possível
- 🟡 médio — claramente identificado como demo mas em rota produtiva
- 🟢 baixo — escopo isolado (storybook, dev-only)

### 1.3 Estados
- ⏳ ativo · 🔄 em substituição (sprint ativa) · ✅ removido

---

## 2. Mocks ativos

| # | Local | Tipo | Risco | Estado | HUB | Notas |
|---|---|---|---|---|---|---|
| MOCK-02 | `/dashboard/whatsapp` inbox parcial | UI-mock | 🟡 | ⏳ | WhatsApp | Mensagens reais chegam ao banco, mas inbox UI ainda exibe mock parcial |
| MOCK-03 | Painel inicial — alguns KPI cards | UI-mock | 🔴 | ⏳ | BI | Mocks misturados com dados reais; sem banner identificando |
| MOCK-04 | `/dashboard/operacoes-v2` mock data inicial dos cards | UI-mock | 🟡 | ⏳ | OS | Hidratação real cobre, mas seed inicial vem de mocks Lovable |
| MOCK-05 | `lib/utils.ts` (componentes Lovable) | API-mock | 🟢 | 🚫 | Lovable | **Aceito** — excluído do tsc por design (CLAUDE.md) |
| MOCK-06 | `/dashboard/cadastros-v2` dados de demo | Seed-mock | 🟢 | ⏳ | CRM | Substituídos quando há dados reais |
| MOCK-07 | Marketplace (HUB inteiro) | Hub-mock | 🟢 | ⏳ | Marketplace | Greenfield — não há código, nem mock |
| MOCK-08 | Marketing IA (campanha, atribuição, ROI) | Hub-mock | 🟡 | ⏳ | Marketing IA | Existe gerador de imagens real, mas orquestrador inexistente |
| MOCK-09 | `/dashboard/contador` — preview restante e DECLARADO no bloco: exportação individual em Relatórios, catálogo de itens de dossiê obtidos fora do sistema, roteiro do CNPJ e preferências das Configurações. Saíram do preview: Fechamento (012), Permissões (014), Obrigações (016), Avisos (017), **Folha & DP (023)** e **resumo do Portal (023)**; **Dossiês (023)** virou híbrida. Nenhuma fixture afirma mais estado da empresa do usuário. | UI-mock | 🟢 | 🔄 | Contador | GOAL 006 realificou **blocos identificados** da Visão Geral e dos Relatórios básicos (read-only por competência). **GOAL 007** substituiu o checklist fictício da seção **Fechamento** por sinais derivados do DTO real (estados ok/atenção/pendente/não disponível; sem percentual inventado; CTA «Fechar competência» desabilitado até GOAL 012). **GOAL 007B** alinhou a semântica: checklist derivado read-only dos dados do GOAL 006 (carga única) — vendas sem movimento ficam **pendentes**; sessões respeitam competência **passada/atual/futura** (via `agora`); **vencimento de títulos permanece não disponível** sem prova agregada; **Fiscal** passa a ter reader A no GOAL 018 (flag off = `nao_disponivel`, nunca zero notas); **Documentos e Conferência** permanecem não disponíveis no checklist quando sem evidência; fechamento oficial permanece **pendente** até o GOAL 012. Não é fechamento real. O card «3 de 9 / 35%» da Visão Geral e o badge do header foram **rotulados como Preview ilustrativo** (não representam o checklist real). O array `FECHAMENTO_CHECKLIST` em `contador-preview-data.ts` está **sem uso runtime** (código morto P3). **GOAL 008** tornou o **Pacote do Contador** real: gerado sob demanda (ZIP com CSVs reais derivados do DTO do GOAL 006 + resumo + índice + avisos/pendências do checklist do GOAL 007 + `manifest.json` com sha256/tamanho de cada arquivo) via endpoint interno autenticado `GET /api/contador/pacote` — **sem storage, sem persistência, sem snapshot**; XML fiscal: GOAL 018 reader A (`CONTADOR_FISCAL_READER` default off + allowlist env-only); placeholder `05-XML/LEIA-ME.md` quando a fonte está `nao_disponivel`; XML UTF-8 persistido só com flag `on` + predicado ADR-007 (HOMOLOGACAO). Documentos anexos no ZIP seguem placeholder honesto. O antigo `PacoteCard`/`PACOTE_ITEMS_*` (lista ilustrativa + botão desabilitado) foi **removido** em favor do download real (`contador-pacote-download.tsx`). **GOAL 008B** reconciliou o contrato do pacote: estrutura fixa de 14 arquivos (00-LEIA-ME/01-VENDAS/02-FINANCEIRO/03-CAIXA/04-DOCUMENTOS/05-XML + manifest.json), **CSVs detalhados por fonte** (linha a linha, RFC 4180 vírgula, PII minimizada) via adapter `carregar-fontes.ts` (carga única read-only, 8 queries), manifesto canônico `omni.contador.pacote.manifest/v1` (storeId só em `competencia.storeId`, período UTC, geradoPor pseudônimo), estados/contagens por fonte, limites + 413, endpoint com validação estrita (rejeita storeId/lojaId na query, `c` inválida → 400) e download **GET direto sem blob**. **GOAL 008C/008D** fecharam a integridade da carga do pacote: paginação real por cursor (páginas de 500), timeout lógico (503), títulos recortados por competência, **limite por fonte aplicado às linhas CRUAS antes da filtragem** (MAX+1 lança `PacoteLimiteExcedidoError` → 413; linha filtrável não burla o teto), **contagem de queries dinâmica** (cada página e cada lote de Produto contam, sem número fixo), **lookup de Produto em lotes de 500** (nenhum `id: { in: [...] }` recebe todos os ids; falha de lote é parcial e explícita, sem contaminar vendas), e **vendas canceladas fora de `vendas.csv`/`itens.csv`** (permanecem apenas no agregado informativo `canceladasTotal`/`canceladasQuantidade` do GOAL 006). Demais seções seguem preview honesto com banner. **Gate G2 aprovado em 2026-07-19** — ADRs 001/003/004/005/006 Accepted em `docs/contador/CONTADOR_HUB_ADRS_PROPOSTOS_001.md`; ajuste **G2-05 (PII)**: CPF, nome, telefone, e-mail, endereço, IMEI e observações **não incluídos por padrão** no domínio/eventos/pacote; **sem toggle automático no GOAL 009**. **GOAL 010** tornou a seção **Documentos** REAL: upload privado direto ao Supabase Storage (bucket `contador-documentos`, privado; intent → PUT assinado → complete), SHA-256 recalculado no servidor, validação de conteúdo (magic bytes / texto), listagem por competência, download autenticado por URL assinada (≤ 300s, attachment), substituição versionada, exclusão soft com motivo e trilha `ContadorEvento` (documento_enviado/substituido/download_autorizado/excluido). Permissão dedicada `hubs.contador` (admin/gerente; caixa/técnico/vendedor negados) substitui `hubs.financeiro` no gate do HUB. A UI da aba Documentos deixou de ser preview (sem PreviewBanner/no-ops). Demais seções seguem preview honesto. **GOAL 011** tornou a seção **Timeline** REAL e deu semântica operacional ao domínio: máquina de status persistida por documento (`pendente→enviado→conferido→resolvido`, com rejeição `enviado|conferido→pendente`), matriz canônica em `lib/contador/status/matriz.ts` (5 transições; qualquer par fora dela falha com `TRANSICAO_INVALIDA`, incluindo transição para o próprio estado); `conferido`/`resolvido` exigem papel financeiro ou administrador (`PERMISSAO_TRANSICAO` → 403) — pendência que a ADR-005 havia deixado em aberto, agora fechada; rejeição exige motivo não vazio (`MOTIVO_OBRIGATORIO` → 422), e o motivo é gravado como **`ContadorComentario` interno imutável** referenciado pelo evento (`motivoComentarioId`/`motivoLen`), nunca como texto livre em `metadata` (ajuste G2-05); `vencido` é **flag derivada** (dia UTC do vencimento < dia de hoje em America/Sao_Paulo, e status ≠ `RESOLVIDO`) — nunca persistida, nunca no enum. Status + comentário + evento `status_alterado` nascem numa **única transação**, com trava otimista (`updateMany` com o estado esperado no `where`) — corrida perdida aborta sem evento órfão. **Comentários** reais (`interna` | `compartilhada`) por competência e por documento, com corte de visibilidade **na consulta** e de novo na projeção (defesa em profundidade): `interna` nunca sai em contexto `compartilhado`. **Timeline** = projeção read-only de `ContadorEvento` + `ContadorComentario`, ordem determinística (mais recente primeiro, desempate estável pelo id composto) e DTO com **allowlist** de chaves de metadata — sem `storageRef`, URL assinada, token, hash, `ip` ou `userAgent`. Rotas novas: `GET/POST /api/contador/status`, `GET/POST /api/contador/comentarios`, `GET /api/contador/timeline` — todas recusam `storeId`/`lojaId`/`papel` vindos do cliente (400) e resolvem escopo por `requireContadorScope` + `auth()`. O array `TIMELINE_ITEMS` e a "Conversa com o contador" mockada foram **removidos**. Seções ainda em preview honesto: Obrigações, Dossiês, Folha, Portal, Permissões, Config e os cartões ilustrativos da Visão Geral. **GOAL 012** tornou a seção **Fechamento** REAL: fechamento transacional com **snapshot canônico imutável** (schema `omni.contador.fechamento.snapshot/v1` — agregados dos readers, checklist, pendências assumidas e contagens de documentos; sem PII, sem `storageRef`, sem URL) e `snapshotHash` SHA-256 sobre JSON **canônico** (chaves ordenadas, decimais e datas normalizados, `NaN`/`Infinity` recusados); **pacote versionado persistido** (`ContadorPacote` + `ContadorPacoteItem`) em path endereçado por conteúdo `contador/{storeId}/{AAAA-MM}/pacotes/v{N}/{manifestoHash}.zip` — upload confirmado **antes** da transação, retry idempotente, blob órfão inalcançável, nunca competência FECHADA apontando para pacote inexistente; **congelamento** da competência `FECHADA` agora cobre upload, substituição, **exclusão lógica**, status **e comentário** (409), mantendo leitura e download liberados (a regra do comentário substitui a hipótese do GOAL 011); **reabertura auditada** com motivo obrigatório gravado como comentário interno imutável, incremento de versão e preservação de pacote/snapshot/eventos anteriores; **detecção de alteração pós-fechamento** com diff determinístico + `diffHash`, avaliada em GET **sem escrita** e persistida só por POST explícito com dedupe por `(competenciaId, versao, diffHash)`; e **comparação de manifestos** entre versões por `sha256`, sem baixar ZIP. Rotas novas: `GET/POST /api/contador/fechamento`, `POST /api/contador/fechamento/reabrir`, `GET/POST /api/contador/fechamento/divergencia`, `GET /api/contador/pacote/versoes`, `POST /api/contador/pacote/download`, `GET /api/contador/pacote/comparar` — todas com lista **única** de chaves proibidas. Seções ainda em preview honesto: Obrigações, Dossiês, Folha, Portal, Permissões, Config e os cartões ilustrativos da Visão Geral. Próximo: **GOALs 013–015** (portal externo) — pré-requisito registrado: filtrar também os **eventos** da timeline no contexto `compartilhado`. **GOAL 012C** integrarou o **storage privado Cloudflare R2** (S3-compatible) como provider oficial dos **documentos** e **pacotes oficiais** do Contador — adapter `lib/contador/documentos/storage-r2.ts` implements `StorageDocumentosPort` (contrato intocado) via `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`; os 4 imports produtivos (`3 routes de upload-intent/complete/[id]/download` + `fechamento/portas.ts`) trocaram `storageSupabase` por `storageR2`; o adapter Supabase (`storage-supabase.ts`) está `@deprecated` (mantido como caminho de rollback manual, sem import produtivo). **Zero dependência produtiva do Supabase Storage** — validado por `grep`. Fail-closed via `StorageConfigError` quando vars R2 ausentes; `StorageProviderError` se `CONTADOR_STORAGE_PROVIDER` ≠ `r2` (sem fallback automático). Hard guard rejeita qualquer secret de storage em `NEXT_PUBLIC_*`. Testes focados do adapter (25) e do config R2 (20) cobrem signed PUT/GET, upload server-side `PutObject`, fail-closed, hard guard, path verbatim (traversal é defendido no service), `Content-Disposition` attachment nunca inline, cap TTL upload 120s/download 300s, e `StorageError` sem vazar token/URL/secret. **Smoke Preview BLOQUEADO por credenciais**: nenhum agente provisionou Cloudflare (`wrangler` não instalado, sem auth), nem Vercel (`vercel login` não rodado) — o ciclo dos 13 passos exige `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET` em Preview da Vercel + bucket private `omni-contador-documentos-preview` no Cloudflare. Nada foi tocado em `omnigestao_prod`, em nenhum Supabase, em nenhum Cloudflare — gate humano conforme GOAL 012B §6. **GOAL 012E** reaplicou a entrega 012/012A/012B/012C sobre a `origin/main` atual e fechou os achados P1/P2/P3 da auditoria independente, **antes** de qualquer provisionamento: (P1) o `complete` passou a exigir um **upload intent assinado (HMAC-SHA256) e expirado** que vincula no servidor loja, usuário, competência, `competenciaId`, `documentoId`, `storageRef`, nome sanitizado, MIME, bytes, SHA-256, categoria e `versaoDeId` — campo vinculado reenviado divergente é **recusado**, o path canônico é comparado por **igualdade estrita** (`storageRefPertence` com `startsWith`/`includes` foi substituída por `storageRefCanonicoConfere`), a idempotência é resolvida **só no repo** e o storage só é tocado depois disso, removendo em falha **apenas** o objeto do intent — fechando o vetor que permitia **abrir e excluir o blob de outro documento**; (P2) o PUT assinado passou a ser de **criação exclusiva** via `If-None-Match: *` (escrita condicional documentada do R2; 2º PUT = 412), com o header dentro do `X-Amz-SignedHeaders` — omiti-lo quebra a SigV4 (403), então a URL não sobrescreve nem depois do `complete`; (P2) o **gate do provider virou produtivo** (`resolverStorageDocumentos` é o único ponto que devolve adapter concreto; nenhuma rota importa `storage-r2`/`storage-supabase`; `CONTADOR_STORAGE_PROVIDER` **sem default** — ausente/vazia falha cerrado, sem fallback Supabase); (P2) `npm run typecheck` virou o gate canônico de tipos (heap de 4 GB — o grafo do `@aws-sdk/client-s3` não cabe nos ~2 GB default e o `tsc` morria por OOM, não por erro de tipo); (P3) `SnapshotFechamentoV1`→`SnapshotFechamentoV2` (schema já era v2), `storeId` saneado também no path do **pacote**, `R2_ACCOUNT_ID` validado em formato (evita apontar credenciais a outro host), comentários de rota que citavam Supabase corrigidos e campos errados nos logs de `pacote/download` (gravava o nome do arquivo em `competencia`) e `fechamento/divergencia` (gravava valor cru do cliente). 36 regressões novas cobrem os 10 itens obrigatórios, incluindo a **reprodução do ataque** e a prova de que ele agora falha **antes de qualquer Head/Get/Delete** (validadas por mutação: revertendo cada guarda, os testes falham). **Provisionamento do R2 segue BLOQUEADO até revisão deste patch** — nenhum bucket criado, nenhuma variável cadastrada, nenhum smoke externo executado. **GOAL 014** tirou a seção **Permissões** do UI-mock: agora é a gestão REAL da identidade externa do contador — `components/dashboard/contador/permissoes/contador-permissoes-real.tsx` consumindo somente as APIs internas `/api/contador-externo/**` (convites por link copiável de uso único exibido uma única vez, listagem de convites sem `tokenHash`, vínculos com suspender/reativar/revogar e suspensão/reativação da identidade como ação elevada com confirmação explícita; estado somente-leitura honesto quando falta `podeGerenciarAcessoExterno` — a API devolve 403).  **GOAL 016** tornou a seção **Obrigações** REAL: obrigações e guias 100% manuais/informadas (sem cálculo fiscal, sem cron), templates com recorrência `mensal`|`nenhuma`, instanciação explícita «Gerar deste mês» (só mensal ativo) e seleção explícita para `nenhuma`, status da obrigação reutilizando a matriz 011, guias com `pagaEm` one-way e associação a documentos 010 da mesma competência. Checklist ganhou o sinal `guias_informadas_vencendo_vencidas` (evidência opcional; 0 guias = `nao_disponivel`, nunca prova ausência de obrigação; falha da agenda não derruba os demais sinais). Migration `0017_contador_agenda` (3 enums + 3 tabelas) em `main` via PR #80 (`abf21166`); apply Production pelo build canônico. **GOAL 017** tornou a **central de avisos** da Visão geral REAL: GET `/api/contador/notificacoes` somente leitura, POST `/avaliar` materializa `alerta_emitido` com dedupe, rascunho pt-BR (copiar) e marcar tratado — **zero envio externo** (`EXTERNAL_SEND_ALLOWED=false`; sem botão Enviar). Canal Meta/Omni permanece classificação B, fora deste GOAL. Corretivo 017-003: `pacote_com_pendencias` consome `manifest.json` da versão efetiva do ZIP oficial (hash `manifestoHash`); falha de leitura é `fontePacote=indisponivel`, nunca lista vazia silenciosa. Demais seções da lista continuam preview.**GOAL 023** (`CONTADOR-HUB-INTERNAL-REALIFICATION-023`) fechou o dashboard INTERNO: nenhuma fixture do HUB afirma mais estado da empresa do usuário. Removidos por completo — **funcionários fictícios** (`FOLHA_FUNCIONARIOS`: Ana Souza / Carlos Lima / Marina Reis, mais o pró-labore "Rafael (titular)" e o holerite ilustrativo), o **escritório contábil de exemplo** ("Escritório Contábil Exemplo · contato@escritorio.com.br · ativo") do Portal, a **empresa de exemplo** das Configurações ("Loja Exemplo Ltda" / "00.000.000/0001-00"), o **resumo financeiro fabricado** da Visão geral (`RESUMO_FINANCEIRO`: R$ 48,2k de vendas etc. — logo ao lado do bloco REAL da mesma competência), os **KPIs fixos** (`VISAO_KPIS`), o **progresso inventado** (`VISAO_DOSSIE_PROGRESS` e o anel "3 de 9 / 35%"), a **situação fabricada por documento** nos dossiês (`atualizado`/`vencido`) e o **Radar CNPJ** que dizia "CND Federal válida" / "Alvará vencido" / "2 pendências e-CAC" sobre um CNPJ que o sistema nunca consultou. Também caiu o **aviso global stale** (`GlobalPreviewNotice`), que afirmava «Nenhum envio, fechamento, guia ou documento é processado» enquanto Documentos (010), Fechamento (012) e Obrigações (016) já eram reais — o preview restante passou a ser identificado **no bloco**, e o pill «Fechamento · preview» saiu do cabeçalho. O **badge numérico `4`** da aba Documentos foi removido (não há contrato leve de contagem sem consulta redundante; número sem leitura é mock) e o campo `count` deixou de existir no catálogo de seções. **Fixtures mortas apagadas** (grep = zero consumo produtivo): `VISAO_ALERTAS`, `OBRIGACOES_ROWS`, `FECHAMENTO_CHECKLIST`, `DOCUMENTOS_ROWS`, `PERMISSOES_ROWS`, `MESES`, `COMPETENCIA_INICIAL`. **Realificado:** **Folha & DP** virou a projeção dos documentos reais de categoria `folha` da competência (`GET /api/contador/documentos?c=…&categoria=folha`, com download autorizado por URL assinada, estados de carga/vazio/erro e microcopy dizendo o que o HUB **não** faz — sem cálculo de folha, holerite, encargos, eSocial, ponto ou cadastro de colaborador; **nenhum schema de Pessoas/RH foi criado**); **Portal do contador** ganhou resumo real do acesso externo pelos mesmos contratos da aba Permissões (`GET /api/contador-externo/acessos` + `/convites`: ativos, convites pendentes, suspensos, revogados, papel e data de concessão, com 403 tratado como "não é seu para ver", nunca como "nenhum contador"), e a lista «o contador pode» foi corrigida para o que o portal v2 faz de fato (deixou de prometer **upload**, que o portal não tem); **Configurações** passou a ler o cadastro persistido da loja ativa (`Store.name`/`Store.cnpj`) por um reader server-side escopado (`lib/contador/readers/loja.ts`) — sem endpoint novo, sem schema novo —, e os controles sem contrato de persistência (regime tributário, competência padrão, switches, botão Salvar) foram **ocultados** e rotulados como planejados; **Dossiês** saiu de PREVIEW total para **HÍBRIDA honesta**: os itens de origem `sistema` com correspondência inequívoca no DTO do GOAL 006 (fluxo realizado, contas a pagar, contas a receber, formas de pagamento) exibem valor real **com a disponibilidade declarada pelo DTO**, os demais dizem "sem fonte no OmniGestão nesta fase" e as origens `portal`/`solicitar` perderam o botão (viraram instrução), enquanto `anexar` ganhou CTA real para a aba Documentos; **Relatórios** trocou os 4 botões de exportação no-op por um índice apontando o arquivo equivalente **dentro do Pacote do Contador** (`01-VENDAS/vendas.csv`, `02-FINANCEIRO/…`, `00-LEIA-ME/resumo.md`), com "Posição de estoque" marcado como **sem fonte** (o DTO do Contador não lê Estoque). **Exportação individual por relatório NÃO foi implementada** (`REPORT_EXPORT_REALIFIED=false`): serviria um arquivo isolado a partir do gerador do pacote, mas exige nova superfície de API/auth no endpoint endurecido `GET /api/contador/pacote` — fora do escopo deste GOAL, e duplicar a lógica de CSV é vedado. **Zero CTA no-op sobrou no HUB**: o helper `noop`, o toast honesto e a constante `CTA_INDISPONIVEL_TITLE` foram removidos por falta de call site — o único `disabled` restante é o do Pacote, condicionado a dado real. Preview declarado que permanece (deliberado, sem contrato): exportação individual em Relatórios, catálogo de itens de dossiê obtidos fora do OmniGestão, roteiro do CNPJ e preferências das Configurações. |

---

## 3. Mocks removidos (histórico)

| # | Local | Removido em | Sprint que removeu |
|---|---|---|---|
| MOCK-01 | `/dashboard/financeiro-v2` (views Lovable) | ~2026-05 (pré-baseline) | migração FinanceiroRealProvider (real-data); confirmado no R0-L5 |

> **Obs. MOCK-01 (R0-L5):** UI plugada a dados reais (sem dataset fake; sem fallback). **DRE / Fluxo de caixa**: dados conectados, mas **evolução visual/funcional pendente** (`ROADMAP_FINANCEIRO`) — maturidade de UI, não mock.

---

## 4. Regras para criar mock (quando inevitável)

1. **Sempre** banner visual no componente: `[DADOS DEMO — não refletem realidade]`.
2. **Sempre** registrar aqui com tipo + risco + sprint para substituição.
3. **Nunca** mock que cria registros no banco "que parecem reais" — ou é real, ou não vai pro banco.
4. **Nunca** mock em fluxo que envolve dinheiro, fiscal ou multi-loja.
5. Se mock vai durar > 1 sprint, vira **dívida técnica** em `DIVIDA_TECNICA.md`.

---

## 5. Verificação periódica

- A cada encerramento de sprint, revisar §2 — algum mock pode ser removido?
- A cada release pública, garantir 0 MOCKs com risco 🔴 ativos.
- Auditoria trimestral varre o código por strings `mock`, `fake`, `dummy`, `seed`, `lorem`.

---

## 6. Fonte da verdade

- **Tracking de mocks:** este arquivo.
- **Dívida (mock que vira dívida):** `DIVIDA_TECNICA.md`.
- **Regra inegociável:** `docs/governance/GOVERNANCA.md` ("nunca criar mocks enganosos").
