"use client"

/**
 * Stub de compatibilidade — o histórico real está em VendasArquivoGeral.
 * Este arquivo existe apenas para não quebrar imports antigos.
 */
export { VendasArquivoGeral as HistoricoVendasClient } from "@/components/dashboard/vendas/vendas-arquivo-geral"
